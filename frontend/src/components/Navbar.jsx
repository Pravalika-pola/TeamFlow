function Navbar() {
  return (
    <nav
      style={{
        background: "#2563eb",
        color: "white",
        padding: "20px",
        borderRadius: "12px",
        textAlign: "center",
        fontSize: "28px",
        fontWeight: "bold",
        marginBottom: "30px",
      }}
    >
      🚀 TeamFlow
    </nav>
  );
}

export default Navbar;